import { Input } from '@arco-design/web-react'
import { optionsProps } from '@/types/form'

const acInputPassword = (props: optionsProps) => {
  return <Input.Password {...props} />
}

export default acInputPassword
